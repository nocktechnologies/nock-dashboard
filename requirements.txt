# MCP server dependency — both the stdio entry point and the Streamable
# HTTP transport mounted at /mcp live inside this Django process. Pulls
# in starlette, sse-starlette, pydantic, uvicorn, etc. as transitive deps.
mcp>=1.23.0,<2.0.0
Django==5.0.4
django-environ==0.11.2
psycopg2-binary==2.9.9
psycopg[binary]==3.2.4
channels==4.1.0
channels-redis==4.2.0
daphne==4.1.2
# Uvicorn is the production ASGI server — Daphne 4.1.2 lacks ASGI
# lifespan support, so the MCP session manager's task group never
# starts under Daphne. Uvicorn ships via mcp's transitive deps but
# pin it directly so production builds don't accidentally drop it.
uvicorn[standard]==0.44.0
celery==5.3.6
redis==5.0.4
django-celery-beat==2.6.0
django-celery-results==2.5.1
django-extensions==3.2.3
requests==2.31.0
python-dotenv==1.0.1
gunicorn==21.2.0
whitenoise==6.6.0
django-tables2==2.8.0
django-filter==24.3
django-fernet-fields-v2==0.9
django-ratelimit==4.1.0
django-axes==7.0.1
django-cors-headers==4.3.1
websockets==16.0
pywebpush==2.3.0
httpx==0.28.1
click==8.3.1
rich==14.3.3
anthropic==0.52.0
djangorestframework==3.15.2
pgvector==0.3.6
openai==2.31.0
pytest==8.3.4
pytest-django==4.8.0
pytest-asyncio==1.3.0
